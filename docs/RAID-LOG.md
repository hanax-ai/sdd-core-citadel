# RAID Log — Amigo Agents

Schema matches `docs/GOAL-AMIGO-AGENTS-DASHBOARD-001.md` section 5.4 (RAID Register). This file is the source of record until `harness/bridge.py` lands and persists it to SQLite.

Fields: `id | type | title | status | owner | phase`

## Risks

| id | title | status | owner | phase |
|----|-------|--------|-------|-------|
| R1 | `gemini-3.6-flash` + `thinking_level=HIGH` + structured schema has never completed a real live call — mocked-only, blocked by free-tier daily quota (20 req/day) | CLOSED — quota reset, live call succeeded 2026-07-30 with correct severity classification and real token counts (43 in / 58 out) | — | Phase4 |
| R2 | `bridge/bridge.py` wildcard CORS (`allow_origins=["*"]`) + zero auth/API-key check on any route, no Origin/Host validation | CLOSED — CORS restricted to the actual Vite dev origins (`localhost`/`127.0.0.1:5173`); every route now requires `X-Bridge-Key` via an app-level dependency, `hmac.compare_digest` comparison, fail-fast at startup if unset. Commit `89c1c96`, 9 new tests | — | Phase4-Dashboard |
| R4 | `bridge/bridge.py` `target_dir` comes straight from the request body (`os.path.abspath` + `os.path.isdir` only, no allowlist) | CLOSED — `BRIDGE_ALLOWED_TARGET_DIRS` allowlist added, same `.resolve()`/`.relative_to()` pattern as `_extract_focus_files`; outside-allowlist requests get 400 `TARGET_NOT_ALLOWED`. Commit `89c1c96` | — | Phase4-Dashboard |
| R5 | `bridge/bridge.py` `HOST` silently overridable via `CITADEL_HOST` env var, defaults to `127.0.0.1` with no warning if changed | OPEN — not addressed by the auth/allowlist fix; auth now provides a real mitigation even if `HOST` is widened, but the silent-override behavior itself is still unchanged | — | Phase4-Dashboard |
| R3 | Live-dev-machine TLS interception (Norton AV) required a local `pip-system-certs` workaround, undocumented for other environments/devs | OPEN | — | Phase4 |
| R6 | `harness/evidence_collector.py` performs zero secret-scanning on file content before it's sent to external LLM providers (Anthropic/OpenAI/Gemini) — a stray API key or credential in a focus file ships raw to all three | CLOSED — native regex-based redaction implemented in `_redact_secrets()` (`harness/evidence_collector.py`), applied to both `file_evidence[].content` and `git_diff_summary` before truncation. Went native regex over `detect-secrets` after checking its actual footprint: core deps are only `pyyaml`+`requests` (light enough), but its API is file/baseline-oriented (built for CLI/pre-commit use against files on disk, not a simple "scan this string" call) — more integration surface for no real gain over a well-scoped pattern set here, so declined in favor of the lighter native approach. Four patterns: AWS access key (`AKIA[0-9A-Z]{16}`), generic credential assignment (`(?i)(api[_-]?key\|secret\|token\|password)(\s*[:=]\s*)(['"])[^'"]{16,}\3`, redacts only the quoted value, keeps the key name for context), PEM private-key block (`-----BEGIN (RSA\|EC\|OPENSSH\|DSA\|PGP )?PRIVATE KEY-----` through its matching `-----END...-----` or to end-of-text if no footer — redacts the full key body, not just the header line, since header-only redaction would leave the actual key material exposed), and JWT-shaped strings (`eyJ...\.[...]\.[...]`). Matches replaced with `[REDACTED-SECRET]`; a `secrets_redacted` boolean is set per file and a `git_diff_secrets_redacted` boolean at the top level. Scanning is wrapped in try/except and degrades to returning the original text unredacted (never crashes evidence collection) on any scan failure. Verified: 5 new tests cover AWS key, generic assignment, private-key block, JWT, and git-diff redaction (all pass); a 6th test confirms a real file (`harness/config.py`, which references `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`OPENAI_API_KEY` via `os.getenv(...)` calls, not assignments) passes through byte-for-byte unmodified with `secrets_redacted: False` — no false-positive redaction on ordinary code. Full suite green (74 passed) after the change. Commit `659db2b` | — | Phase4 |

## Assumptions

| id | title | status | owner | phase |
|----|-------|--------|-------|-------|
| A1 | Provider SDK close/timeout methods (`AsyncAnthropic.close()`, `AsyncOpenAI.close()`, `client.aio.aclose()`) are real and safe to call | CLOSED — verified via live SDK introspection, not guessed | — | Phase4 |
| A2 | Propose-only guarantee (Builder never writes files) is a sufficient safety boundary | CLOSED — now actually exercised: `execute_run()` calls the real `run_collaboration_cycle`, and `test_bridge.py` includes a propose-only write-guard test against the real path, not the mock. Holds | — | Phase4-Dashboard |
| A3 | Task-description text is trusted input | CLOSED — was false; path-traversal fix (`_extract_focus_files`) proved arbitrary task text could read files outside `target_dir`. Treat as a standing reminder: any user-supplied text reaching filesystem logic needs the same scrutiny going forward | — | Phase4 |

## Issues

| id | title | status | owner | phase |
|----|-------|--------|-------|-------|
| I1 | No clean live `PASS` verdict on record yet for the current Gatekeeper config (blocked by R1) | CLOSED, fully — real end-to-end `run_collaboration_cycle` through the actual bridge process, 2026-07-31 (`run_3656145f15`): `PASS`, 1 round, 8,091 real tokens, correct docstring patch, SSE stream verified event-for-event against the DB transcript, propose-only held (`git diff` empty on the target file). Nothing left unverified on this item | — | Phase4 |
| I2 | Dashboard's Vite proxy SSE snippet strips `content-encoding` without decompressing the body first | CLOSED — proxy added to `vite.config.ts` with the correct fix: forces `Accept-Encoding: identity` on the proxied request instead of stripping the response header after the fact. Commit `1bedbff` | — | Phase4-Dashboard |
| I3 | Spec's planned SQLite persistence needs async-safe I/O | CLOSED — every DB write now runs through `asyncio.to_thread` via sync helper functions; `Run.emit()` is a cheap non-blocking sync call that fans out to a per-run queue drained by one dedicated task (avoids a race the implementing agent's own test caught: concurrent per-event `create_task` writes landing out of `seq` order). Commit `89c1c96` | — | Phase4-Dashboard |
| I4 | `bridge.py`'s `execute_run()` never imports or calls `harness.remediation_loop.run_collaboration_cycle` at all — it's a hardcoded mock | CLOSED — rewritten to call the real function with real `target_dir`/`task_description`/`run_id`/`on_event`. Commit `89c1c96` | — | Phase4-Dashboard |
| I5 | `bridge.py`'s `Run.emit()` is `async def`, but the real `on_event` contract is a plain sync `Callable[[dict], None]` | CLOSED — `emit()` is now a plain sync function; blocking persistence handed off to a per-run drain task via a queue rather than awaited inline. Commit `89c1c96` | — | Phase4-Dashboard |
| I6 | `bridge.py`'s `asyncio.create_task(execute_run(run))` return value is discarded, no reference retained anywhere | CLOSED — retained in a module-level `_BACKGROUND_TASKS` set with a `done_callback`; harness exceptions are caught inside `execute_run` itself and turned into a persisted `error` event + `ERROR` verdict, not a silent death. Commit `89c1c96` | — | Phase4-Dashboard |
| I7 | `bridge.py`'s emitted event shape doesn't match the real contract at all | CLOSED — moot once I4 was fixed; events are relayed verbatim, no renaming. Verified in `test_bridge.py` (real field names, real emission order) | — | Phase4-Dashboard |
| I8 | `bridge.py`'s `runs` table UPDATE never sets `patch_text` or `tokens_total` | CLOSED — `tokens_total` accumulated from every `token_metric` event, `patch_text` captured from `run_complete`, both persisted on completion. Verified by test (43 = 10+5+20+8). Commit `89c1c96` | — | Phase4-Dashboard |
| I9 | Frontend `contract.ts`'s zod schemas don't match the real event contract in 4 places | CLOSED — `token_metric` now uses `input_tokens`/`output_tokens`, `severity` enum corrected to `CRITICAL/WARNING/NOTE`, `agent_message` uses a `.refine()` so `content` is required except for `AUDIT_FINDINGS` (which requires `findings` instead), `stage_change.agent`/`round`/`verdict` now optional. Verified by `tsc --noEmit` clean plus a runtime parse check of all 18 rewritten demo events. Commit `1bedbff` | — | Phase4-Dashboard |
| I10 | Frontend `fixtures.ts` (Demo mode) was built to match the frontend's own wrong contract | CLOSED — `DEMO_TRANSCRIPT` fully rewritten to the real contract; verified all events parse against the corrected schema | — | Phase4-Dashboard |
| I11 | Frontend assumes an `error` SSE event the real harness never emits; unbounded 3s reconnect retry loop | CLOSED — turned out to be a non-issue on the shape (bridge.py does emit `{"type": "error", "error_code": ..., "message": ...}` at the bridge boundary, matching the frontend's existing `runErrorSchema` exactly). Reconnect loop fixed regardless: exponential backoff 3s→30s cap, max 8 attempts, then a `connection: "lost"` state with a manual reconnect action instead of retrying forever. Commit `1bedbff` | — | Phase4-Dashboard |
| I12 | Auth integration gap between the two parallel fixes: `bridge.py` requires `X-Bridge-Key` on every route, but browser-native `EventSource` can't set custom headers; frontend had no bridge-key concept at all | CLOSED — query-param fallback (`?key=`) added; `bridgeKey` added to the persisted UI store, wired into every fetch call and the SSE URL; masked key-entry input added to `ConnectionControl.tsx`. Commit `ce4b91c`. **Refined by CodeRabbit, commit `fd3df31`**: the query fallback had been applied globally (every route, not just SSE) and had a header-precedence bug (`x_bridge_key or key` treated an empty header as absent). Fixed: query fallback now scoped to `GET /api/stream/{run_id}` only (per-route `Depends`, 6 other routes stay header-only), precedence fixed to `is not None`. Verified end-to-end against a real running process both times — curl auth matrix, real browser `EventSource` `readyState` checks, and (for the refinement) a direct regression test proving `/api/logs?key=<valid>` with no header now correctly 401s | — | Phase4-Dashboard |
| I13 | `bridge.py`'s CORS allowlist hardcodes port 5173 (Vite's default), but this dev environment's Vite server actually runs on 8080 | OPEN — found incidentally during I12's live verification, out of scope for that task. Low urgency (doesn't block anything, sandbox-specific), but the allowlist should probably read the port from an env var rather than hardcoding a default | — | Phase4-Dashboard |
| I14 | `evidence_collector.py` truncates file content and git diff by raw character count (4000/2000 chars), not token count | CLOSED — replaced flat char-slicing with `tiktoken`'s `cl100k_base` encoding (added to `requirements.txt`, `tiktoken>=0.7.0`; core deps are just `regex`+`requests`, no heavy footprint) as a single universal approximation across the three providers, not per-provider-exact tokenization. Budgets: 1800 tokens for file content, 1000 tokens for git diff (same ballpark as the old 4000/2000-char limits by design). `_truncate_to_tokens()` encodes, slices on the token boundary, and decodes back to text; falls back to a rough ~4-chars/token char-based cutoff if `tiktoken`'s encoder fails to load (e.g. no network access to fetch the `cl100k_base` BPE file on first use), so evidence collection can't hard-fail on this. `content_truncated`/`git_diff_truncated` flags now reflect the real post-redaction token count. Verified with 4 new tests built from measured `tiktoken` output: a dense low-char/high-token fixture (2250 chars / ~2001 tokens for file content, 1170 chars / ~1041 tokens for diff) now truncates despite being well under the old 4000/2000-char thresholds, proving char-based truncation is actually gone; a sparse high-char/low-token prose fixture (4050 chars / ~901 tokens for file content, 2250 chars / ~501 tokens for diff) does NOT truncate despite exceeding the old char thresholds, proving the fix isn't the old behavior with an extra dependency bolted on. Two pre-existing tests that asserted exact 4000/2000-char truncation lengths on repeated-character fixtures (`"x"*5000`, `"x"*2500`) were updated in place — those fixtures tokenize very cheaply under BPE (625 and 313 tokens respectively) and correctly no longer truncate under token-aware logic, which is itself a real demonstration of why char-based truncation was crude. Full suite green (74 passed). Commit `659db2b` | — | Phase4 |
| I15 | `harness/llm_clients.py`'s three `call_*` functions have no fallback on quota/rate-limit exhaustion — a `RuntimeError` fails the whole run outright | CLOSED — fallback mechanism implemented and tested 2026-07-31 (`aa5f068`). SDK introspection first (per this repo's standing practice, not guessed): `anthropic.RateLimitError` and `openai.RateLimitError` are both `APIStatusError` subclasses carrying a reliable `status_code = 429`; `google.genai` has no dedicated rate-limit exception, so Gemini quota detection checks `APIError.code == 429` or `.status == "RESOURCE_EXHAUSTED"` (matches the real exhausted-quota shape seen in R1). Each `call_*` now catches the provider's quota-specific exception separately from its general error class; when caught, and `<ROLE>_FALLBACK_BASE_URL`/`<ROLE>_FALLBACK_MODEL` (`RESEARCHER_`/`BUILDER_`/`GATEKEEPER_`) are both set, the same request retries once against the fallback endpoint using the same SDK client class (`base_url=...` for Anthropic/OpenAI, `HttpOptions(base_url=...)` for Gemini), and the returned dict gains `fallback_used: true` / `fallback_reason: "quota_exceeded"` so callers can tell a degraded fallback answer from a real one. No fallback configured (today's default — no fallback vars in `.env.example`) raises the same `RuntimeError` as before, now with quota-specific wording distinct from other failures. Verified a non-quota error (auth error) never triggers the fallback path in any of the three providers — only one client construction, generic (non-quota) error message. 9 new tests added to `tests/test_llm_clients.py` (3 per provider: no-fallback quota error, fallback retry success, non-quota error skips fallback); full suite green, no regressions. Explicitly NOT built: standing up LocalAI itself (separate infra decision, still undecided) and multi-key round-robin/load-balancing (out of scope per D5 — Dify's `LBModelManager` pattern is not a peer substitute, one labeled fallback attempt is the whole scope) | — | Phase4 |
| I16 | Claude-in-Chrome MCP extension had real friction during tonight's CITADEL preview review: Lovable's `lovable.dev/preview/` wrapper intercepted clicks (fixed by navigating directly to the underlying `id-preview--*.lovable.app` URL instead), plus repeated CDP `Page.captureScreenshot` timeouts requiring retries | OPEN — not a blocker (worked once routed around), but a concrete, first-hand data point for D6's comparison, not just an opinion | — | Phase4 |
| I17 | `logs/` (the CLI/`remediation_loop.py`'s own JSON transcript output, separate from `bridge.py`'s SQLite storage) is not gitignored — 5 real transcripts from tonight's live testing are already tracked, each containing real evidence payloads (file contents, research notes, patches, findings) | OPEN, needs a decision — same category of question already resolved for `docs/`/`reviews/`/`.codegraph/` (user-directed). Given the Hana-X flagship framing, probably shouldn't accumulate arbitrary run transcripts in git history by default; user to decide whether to gitignore going forward and/or untrack the 5 existing ones | User | Phase4 |

## Dependencies

| id | title | status | owner | phase |
|----|-------|--------|-------|-------|
| D1 | Lovable AI to deliver full CITADEL dashboard codebase (zip) | IN PROGRESS | User | Phase4-Dashboard |
| D2 | Gemini quota reset or billing enabled, to close R1/I1 | OPEN | User | Phase4 |
| D3 | Harness core async-readiness (Tasks 6-7 of the async rewrite) is the prerequisite `bridge.py` needs to avoid blocking its own event loop — this dependency is now satisfied | CLOSED | — | Phase4-Dashboard |
| D6 | browser-use (`gregpr07`/`browser-use`, now `browser-use/browser-use`) researched as a candidate browser-automation tool | OPEN — not installed yet. Legit and hugely popular (107.4k stars), Python pip-installable, Playwright-based, supports Claude/GPT/Gemini via BYOK. Redundant for my own interactive use (Claude-in-Chrome MCP tools already do this) EXCEPT the user's stated preference is real, not just taste: see I16 for the concrete friction (iframe click interception, CDP screenshot timeouts) hit tonight with the Chrome extension. For the harness itself, doesn't plug in without giving an agent role an actual tool-calling loop first (today's `call_*` functions are one-shot, no tool loop to attach a browser action to) — real use case identified: automated visual verification of the CITADEL dashboard as part of Gatekeeper review or a new evidence step. One unresolved question before adopting: whether BYOK mode keeps all data fully local or touches browser-use's own cloud for anything — their README didn't say, would need the actual privacy policy checked | User | Phase4 |
| D4 | Repomix (`yamadashy/repomix`) evaluated as a candidate to enhance `evidence_collector.py` | CLOSED, declined — legit tool (Tree-sitter compression, Secretlint, token counting) but Node.js-only (no Python API) and designed for whole-repo packing, mismatched against our targeted-focus-file, cost-conscious collection pattern. Would add a hard cross-language runtime dependency for gains (token-aware truncation, secret-scanning) that are small, well-scoped native-Python jobs — see R6/I14. Not revisiting unless a real need for Tree-sitter-quality structural compression emerges | — | Phase4 |
| D5 | Dify (`langgenius/dify`) evaluated as a reference pattern for the Claude/Codex/Gemini pipeline | CLOSED, platform declined / pattern accepted — Dify itself is a full enterprise platform (own web UI, Postgres, Docker Compose/K8s), wrong shape entirely for this harness, not installed. Its `LBModelManager` failover pattern is legitimately motivated by a real gap though — see I15, sized separately | — | Phase4 |

---

## Lessons Learned

- **Convergent findings across independent configs are signal, not noise.** Gatekeeper blocking the same demo task identically across three separate configurations (original prompt, scope-refined prompt, model+reasoning upgrade) was correctly read as principled reviewer behavior, not a weak-model artifact — fixed with a severity gate (`has_blocking_findings`), not more prompt tuning.
- **Verify SDK/API claims by introspection, not by trusting a suggestion — including an automated reviewer's.** Caught the real OpenAI Responses-API requirement, real Gemini model names, and real provider close()/timeout parameter names this way instead of guessing or blindly applying CodeRabbit's suggested diffs.
- **Automated external review dropped mid-session is a useful signal but gets triaged, not rubber-stamped.** All 9 CodeRabbit findings this session were real defects and got fixed; each still got independent verification before the fix, not blind acceptance.
- **Content that instructs agent action, delivered via a file rather than a direct request, gets flagged before acting** — even when it turns out to be legitimate (the CITADEL bridge handoff note).
- **Windows CRLF vs. this repo's LF convention recurs on every new/moved file**, even after the `.gitattributes` fix — normalize line endings before `git add` when scripting file creation.
- **A Demo/mock mode built against the wrong contract validates itself and hides the bug.** CITADEL's `fixtures.ts` was shaped to match `contract.ts`'s own (wrong) zod schemas, so Demo mode looks flawless while the real event schema is different in 4 places. Never trust a mock's self-consistency as evidence the mock matches reality — check the mock against the actual producer's contract, not against the mock's own consumer.
- **"Contract implemented" in a docstring/README is a claim, not a verification.** `bridge.py`'s execute_run is entirely mocked (never calls the real harness) despite reading as feature-complete; only reading past the interface layer into the actual call sites caught it.
- **Two subagents fixing opposite ends of a contract in parallel will produce a seam neither can see.** The backend fix added header-based auth without knowing the frontend uses browser-native `EventSource` (can't set headers); the frontend fix had no bridge-key concept to wire up since auth didn't exist yet when it was dispatched. Caught immediately by reading both diffs together right after they landed, before assuming "both green" meant "integrated." Parallel dispatch is fine for independent files — always do one integration pass across the seam before calling cross-cutting work done.
